import { CurrentLocation } from "@/type";

type Props = {
  location: CurrentLocation | undefined;
};

const TravelContainer = ({ location }: Props) => {
  return <div>TravelContainer</div>;
};

export default TravelContainer;
